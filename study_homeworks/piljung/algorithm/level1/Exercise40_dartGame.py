# 40. [1차] 다트게임

import re


def solution(dartResult):
    score = re.findall('\d+', dartResult)  # 숫자만 추출하는 방법
    bonus = re.findall("[a-zA-Z]+", dartResult)  # 문자만 추출하는 방법

    for n in range(len(bonus)):

    print(a)
    print(b)

    answer = 0
    return answer